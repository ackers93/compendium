<% if commentable.is_a?(BibleVerse) %>
  <% count = commentable.visible_comments.count %>
<% else %>
  <% count = commentable.comments.count %>
<% end %>

<% if count > 0 %>
  <span class="comment-count-badge">
    <%= pluralize(count, 'comment') %>
  </span>
<% end %>
